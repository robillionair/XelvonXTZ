{
  "name": "xelvon-xpt",
  "version": "1.0.0",
  "description": "Xelvon XPT",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "esbuild ./server.ts --bundle --platform=node --format=cjs --packages=external --outfile=dist/server.cjs",
    "start": "node dist/server.cjs"
  },
  "dependencies": {
    "@google/genai": "^2.10.0",
    "express": "^4.21.2",
    "firebase": "^12.15.0",
    "firebase-admin": "^14.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.2"
  }
}
